import areas from '@/modules/storage/areas';
import goals from '@/modules/storage/goals';
import theme from '@/modules/storage/theme';

const storage = {
    areas,
    goals,
    theme,
};

export default storage;
