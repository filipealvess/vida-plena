import { BrowserRouter } from "react-router-dom";
import Router from "@/core/Routes";

function App() {
    return (
        <BrowserRouter>
            <Router />
        </BrowserRouter>
    );
}

export default App;
